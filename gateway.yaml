server:
  port: 8059
spring:
  application:
    name: gateway
  cloud:
    discovery:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
        lowerCaseServiceId: true
#  slueth:
#    sampler:
#      probability: 1.00
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka